package com.specialty.bookstore.catalogmanagement.interfaces;

import com.specialty.bookstore.catalogmanagement.model.Book;

public interface IBookService {
    
    public Book addBook(Book book);
}
